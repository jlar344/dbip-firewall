# Managed by Ansible — do not commit real secrets.
# Generate: openssl rand -hex 32
DBIP_SECRET={{ dbip_secret | mandatory }}
DBIP_SSH_HOST={{ dbip_ssh_host | default(inventory_hostname) }}
DBIP_SSH_PORT={{ dbip_ssh_port }}
DBIP_SESSION_MINUTES={{ dbip_session_minutes }}
